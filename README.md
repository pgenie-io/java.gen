# java.gen

A [pGenie](https://github.com/pgenie-io/pgenie) plugin that generates type-safe Java bindings for PostgreSQL based on JDBC with support for most of the PostgreSQL data types including arrays, composites and multiranges.
